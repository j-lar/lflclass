#!/usr/bin/perl

## Exercise A
$y = $x < 0 ? 5 : 10;

##Exercise B
$lastname = ( $fname eq "Joe" ) ? "Smoe" : "Smith";

##Exercise C
$size = ( $length < 12 ) ? "short" : ( $length < 24 ) ? "medium" : "long";
